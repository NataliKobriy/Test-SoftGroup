{% extends "layout.twig" %}
{% block content %}

        {% for generals in data %}
            <div class="text">
                <img src="/app/views/photos/{{ generals.photos }}" alt="{{ generals.photos }}" class="img-rounded">
            {% autoescape false %}
                {{ generals.bibliographic }}
            {% endautoescape %}

        {% endfor %}
    <div class="href">
        {% for generals in data %}
            <a href="/about/index?id={{ prev.id_generals }}" style="float: left">{{ prev.surname }}</a>
            <a href="/">Головна сторінкa</a>
            <a href="/about/index?id={{ next.id_generals }}" style="float: right">{{ next.surname }}</a>
        {% endfor %}
    </div>
    </div>

{% endblock %}
